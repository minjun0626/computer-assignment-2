<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>단일 파일 템플릿</title>
    
    <style>
        /* 변수 정의 (테마 전환용) */
        :root {
            --bg-color: #f4f4f9;
            --text-color: #333333;
            --card-bg: #ffffff;
            --accent-color: #4a90e2;
        }

        [data-theme="dark"] {
            --bg-color: #1e1e2f;
            --text-color: #f4f4f9;
            --card-bg: #2a2a40;
            --accent-color: #ffcc00;
        }

        /* 기본 리셋 및 레이아웃 */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            transition: background-color 0.3s, color 0.3s;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: var(--bg-color);
            color: var(--text-color);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        /* 카드 컴포넌트 */
        .card {
            background-color: var(--card-bg);
            padding: 2.5rem;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            text-align: center;
            max-width: 400px;
            width: 100%;
        }

        h1 {
            margin-bottom: 1rem;
            font-size: 1.8rem;
        }

        p {
            margin-bottom: 2rem;
            color: #777;
            line-height: 1.5;
        }

        /* 테마 변경 버튼 */
        .toggle-btn {
            background-color: var(--accent-color);
            color: #fff;
            border: none;
            padding: 0.8rem 1.5rem;
            font-size: 1rem;
            font-weight: bold;
            border-radius: 30px;
            cursor: pointer;
            outline: none;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .toggle-btn:hover {
            opacity: 0.9;
            transform: translateY(-1px);
        }

        .toggle-btn:active {
            transform: translateY(1px);
        }
    </style>
</head>
<body>

    <main class="card">
        <h1>Vanilla JS 템플릿</h1>
        <p>HTML5, CSS3, 그리고 순수 자바스크립트가 하나의 파일로 결합된 가볍고 강력한 구조입니다.</p>
        <button id="themeToggle" class="toggle-btn">🌙 다크 모드 켜기</button>
    </main>

    <script>
        // DOM 요소 선택
        const themeToggleBtn = document.getElementById('themeToggle');
        
        // 버튼 클릭 이벤트 리스너 등록
        themeToggleBtn.addEventListener('click', () => {
            // 현재 html 태그의 data-theme 속성 확인
            const currentTheme = document.documentElement.getAttribute('data-theme');
            
            if (currentTheme === 'dark') {
                // 라이트 모드로 전환
                document.documentElement.removeAttribute('data-theme');
                themeToggleBtn.textContent = '🌙 다크 모드 켜기';
            } else {
                // 다크 모드로 전환
                document.documentElement.setAttribute('data-theme', 'dark');
                themeToggleBtn.textContent = '☀️ 라이트 모드 켜기';
            }
        });
    </script>
</body>
</html>